# Desktop (Tauri) — Cloudflare port notes

Retarget auth/API base URLs to the OpenNext Worker origin (local `http://localhost:8788` or production worker URL).

Do not bundle Firebase. Use Better Auth session cookies against the web API.
