{
  "name": "@template/widget",
  "version": "0.1.0",
  "private": true,
  "description": "Embeddable visitor chat widget \u2014 preact + esbuild, IIFE bundle served from apps/web/public/widget/",
  "scripts": {
    "build": "node scripts/build.mjs && node scripts/check-size.mjs",
    "build:dev": "node scripts/build.mjs --dev",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "lint": "echo 'widget package skips eslint \u2014 bundled separately'"
  },
  "dependencies": {
    "preact": "^10.27.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.5",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  },
  "peerDependencies": {}
}
